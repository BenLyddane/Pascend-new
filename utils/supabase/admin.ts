// This file is no longer used. We're using the createClient function from server.ts instead.
// Keeping this file as a placeholder to avoid breaking imports.

import { createClient } from './server';

export const createAdminClient = createClient;
